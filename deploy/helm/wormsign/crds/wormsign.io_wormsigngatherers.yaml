---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: wormsigngatherers.wormsign.io
spec:
  group: wormsign.io
  names:
    kind: WormsignGatherer
    listKind: WormsignGathererList
    plural: wormsigngatherers
    shortNames:
    - wg
    singular: wormsigngatherer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          WormsignGatherer defines a custom diagnostic data collector. Gatherers
          collect Kubernetes resource data and logs relevant to a fault event.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the gatherer configuration.
            properties:
              collect:
                description: Collect defines the list of data collection actions to
                  perform.
                items:
                  description: CollectAction defines a single data collection operation.
                  properties:
                    apiVersion:
                      description: |-
                        APIVersion is the Kubernetes API version of the resource to collect.
                        Required when type is "resource".
                      type: string
                    container:
                      description: |-
                        Container is the name of the container whose logs to collect. Only
                        used when type is "logs".
                      type: string
                    jsonPath:
                      description: |-
                        JSONPath is an optional JSONPath expression to extract specific fields
                        from the resource. Only used when type is "resource".
                      type: string
                    listAll:
                      description: |-
                        ListAll, when true, lists all resources of the specified type in the
                        namespace rather than fetching a single named resource. Only used when
                        type is "resource".
                      type: boolean
                    name:
                      description: |-
                        Name is the name of the specific resource to fetch. Supports template
                        variables: {resource.name}, {resource.namespace}, {resource.kind},
                        {resource.uid}, {node.name}. Required when type is "resource" and
                        listAll is false.
                      type: string
                    namespace:
                      description: |-
                        Namespace is the namespace of the resource to fetch. Supports template
                        variables. Defaults to the fault event resource's namespace.
                      type: string
                    previous:
                      description: |-
                        Previous, when true, collects logs from the previous terminated
                        container instance. Only used when type is "logs".
                      type: boolean
                    resource:
                      description: |-
                        Resource is the lowercase plural resource name to collect (e.g.,
                        "pods", "destinationrules"). Required when type is "resource".
                      type: string
                    tailLines:
                      description: |-
                        TailLines is the number of log lines to collect from the end. Only
                        used when type is "logs".
                      format: int64
                      maximum: 10000
                      minimum: 1
                      type: integer
                    type:
                      allOf:
                      - enum:
                        - resource
                        - logs
                      - enum:
                        - resource
                        - logs
                      description: Type is the kind of collection to perform.
                      type: string
                  required:
                  - type
                  type: object
                minItems: 1
                type: array
              description:
                description: |-
                  Description is a human-readable explanation of what this gatherer
                  collects.
                maxLength: 1024
                type: string
              triggerOn:
                description: TriggerOn specifies which fault events activate this
                  gatherer.
                properties:
                  detectors:
                    description: |-
                      Detectors is a list of detector names (built-in or custom) that
                      trigger this gatherer when they emit a fault event.
                    items:
                      type: string
                    type: array
                  resourceKinds:
                    description: |-
                      ResourceKinds is a list of Kubernetes resource kinds (e.g., "Pod",
                      "Node") that trigger this gatherer when they appear in a fault event.
                    items:
                      type: string
                    minItems: 1
                    type: array
                type: object
            required:
            - collect
            - triggerOn
            type: object
          status:
            description: Status contains the observed state of the gatherer.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  resource's current state. Standard condition types are Ready, Valid,
                  and Active.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
