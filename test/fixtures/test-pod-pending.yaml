apiVersion: v1
kind: Pod
metadata:
  name: stuck-pod
  namespace: default
  labels:
    app: test-app
spec:
  containers:
    - name: app
      image: nginx:1.25
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
