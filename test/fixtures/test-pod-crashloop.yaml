apiVersion: v1
kind: Pod
metadata:
  name: crashloop-pod
  namespace: default
  labels:
    app: crashloop-test
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "exit 1"]
